apiVersion: 1
providers:
  - name: TalentUPQ
    folder: TalentUPQ
    type: file
    options:
      # /var/lib/grafana tiene un disco persistente en Render. Los dashboards
      # aprovisionados viven en /etc para que el montaje no los oculte.
      path: /etc/grafana/dashboards
